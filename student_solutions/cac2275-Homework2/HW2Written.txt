Christian Cruz Godoy
Cac2275
Homework 2 
Written


P1.)


Public static AnyType printLots(List<AnyType> elements, List<Integer> positions)
{
        boolean isMatched = false;
	int elPos = -1;
        //This is supposed to allow the use of iterators for my lists
        Iterator itrEle = elements.iterator();
        Iterator itrPos = positions.iterator();


        //this method is going to compare the element in my list for
        //positions to my actual position
        while(itrPos.hasNext())
        {
        	int pos = itsPos.next();
                	while(itsEle.hasNext() && !isMatched)
                	{
       			   AnyType z = itrEle.next();
       			   elPos++;
        
        		   if(elPos == pos)
        		   {
        			isMatched = true;
        			Return z;
			   }
			}
	}
}




P2.)


Iterator one = L1.iterator();
Iterator two = L2.iterator();
Iterator three = L3.iterator();

while(one.hasNext())
{
        firstElement = one.next();
        Boolean isMatched = false;
        
        while(!isMatched && two.hasNext())
        {
                secondElement = second.next();
                
                if(firstElement == secondElement)
		 {
       			 isMatched = true;
        
        		if(!three.hasNext())
       			{
        	          L3.add();
			}
		 }
		 
		 else
		 {
        	     Boolean isInList = false
        	     while(three.hasNext() && !isInList)
       		     {
        		Element k = three.next();
        		if(firstElement == k)
			{
        		    isInList = true;
       			    iterator.reset();
			}
			Else if(!three.hasNext() && !isInList)
			{
       			    L3.add();
       		           iterator.reset();
        		   isInList = true;
			}
		     }
		 }	
        }
}


P3.)
topOne = 0;
topTwo = arr.length() – 1;
firstCount = 0;
secondCount = 0;
Boolean overFlow;

Public static firstStackAdd(AnyType x)
{
        If(firstCount + secondCount ==arr.length() + 1)
	{
                overflow = true;
        }
        Else
	{
                arr[topOne] = x;
                firstCount++;
                secondCount++;
        }
}


Public static secondStackAdd(AnyType y)
{
        If(firstCount + secondCount ==arr.length() + 1)
	{
                overFlow = true;
        }
        Else
	{
                arr[topTwo] = y;
                topTwo;
                secondCount++;
        }
}


 Public static stackOneRemove()
 {
                firstTop--;
                array[firstTop] = null;
                firstCount--;
 }






Public static secondStack()
{
                secondTop++;
        array[secondTop] = null;
                secondCount--;
        
}


public static boolean firstStackIsEmpty()
{
        if (firstCount ==0)
                return true;
        else
                return false;
}


public static boolean secondStackIsEmpty()
{
        if (secondCount ==0)
                return true
        else
                return false
}


P4.)
a.)
1. Move 4 to S1
2. Move 3 to S2
3. Move 1 to Output track
4. Move 8 to S3
5. Move 2 to Output Track
6. Move 3 to Output Track
7. Move 4 to Output Track
8. Move 7 to S1
9. Move 6 to S1
10. Move 9 to S2
11. Move 5 to Output Track
12. Move 6 to Output Track
13. Move 7 to Output Track
14. Move 8 to Output Track
15. Move 9 to Output Track
b.) 1,9,2,8,3,7,4,6,5