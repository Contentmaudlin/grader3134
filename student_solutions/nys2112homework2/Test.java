/*
 * Natalie Sayegh
 * October 10
 * Programming 1
 * Java file for testing SymbolBalance.java with unbalanced */

public class Test {
	for(int i = 2; i<5; i++) {
		/*do stuff*/
	/*this comment never ends.
	 * As a result, the symbols are unbalanced
	 * (because the closing symbols get ignored),
	 * but the error is an unclosed comment
	 }
}

