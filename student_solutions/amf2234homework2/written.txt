Problem 1.public static <AnyType> void printLots(List<AnyType> l, List<Integer> p) {    int i = 0;    java.util.Iterator<AnyType> n = l.iterator();    while (n.hasNext()) {        if (p.contains(i))            System.out.println(n.next());        else            n.next();        i += 1;    }}3.4List<Integer> intersection = new List<Integer>()java.util.Iterator<Integer> n = L1.iterator()while n.hasNext() {    item = n.next()    if (L2.contains(item) == true) and (intersection.contains(item) == false) {        intersection.add(item)}3.24public class TwoStacks<AnyType> {private AnyType[] arrayprivate int maxSizeprivate int top1private int top2public TwoStacks() {    array = (AnyType[]) new Object[maxSize]    maxSize = 100    top1 = 0    top2 = maxSize}public void pushS1(AnyType element) {    //push S1 elements to the front of the list going forward    if (top1 + (maxSize – top2) < maxSize)        array[top1] = element        top1 ++	    else	        throw StackOverflowException}public void pushS2(AnyType element) {    //push S2 elements to the back of the list going backward    if (top1 + (maxSize – top2) < maxSize)        top2 --        array[top2] = element    else        throw StackOverflowException}public AnyType popS1() {    if (top1 > 0)        AnyType popped = array[top1]        top1 --  //don’t have to “delete” element, just replace it later        return popped     else        return null //if the array is empty}public AnyType popS2() {    if (top2 < maxSize)        AnyType popped = array[top2]        top2 ++  //don’t have to “delete” element, just replace it later        return popped     else        return null //if the array is empty}}Problem 4a.)1. Move car 4 to holding track S12. Move car 3 to holding track S13. Move car 1 to the output track4. Move car 8 to holding track S25. Move car 2 to the output track6. Move car 7 to holding track S27. Move car 6 to holding track S28. Move car 9 to holding track S39. Move car 5 to holding track S310. Move car 3 to the output track11. Move car 4 to the output track12. Move car 5 to the output track13. Move car 6 to the output track14. Move car 7 to the output track15. Move car 8 to the output track16. Move car 9 to the output trackb.) A train with cars in the order [2, 3, 4, 5, 6, 7, 8, 9, 1] cannot be arranged in increasing order with three holding tracks.